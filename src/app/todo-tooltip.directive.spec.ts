import { TodoTooltipDirective } from './todo-tooltip.directive';

describe('TodoTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
